// index.js

import React from "react";
import {createRoot} from "react-dom/client";
import App from "./App";

// Render the App componenet into the root DOM
createRoot(document.getElementById("root")).render(<App />);